use array::ArrayTrait;
use quaireaux::data_structures::linked_list::LinkedListTrait;
use integer::u32_to_felt;
use debug::print_felt;

#[test]
#[available_gas(1000000000)]
fn linked_list_new_test() {
    let linked_list = LinkedListTrait::new();
    // let length = linked_list.length;
    // print_felt(u32_to_felt(length));
// arr.append(2);
// arr.append(4);
// arr.append(6);
// arr.append(8);
// arr.append(10);
// assert(gcd_of_n_numbers::gcd(ref arr) == 2, 'invalid result');
}
